Unauthorized UDeploy agent detected on the following server(s):

${BUILD_LOG_REGEX, regex="^[a-zA-Z0-9._-]+( [a-zA-Z0-9._-]+)*$", maxMatches=1, showTruncatedLines=false}

Job Name: ${JOB_NAME}
Build Number: ${BUILD_NUMBER}
Build URL: ${BUILD_URL}

Please investigate immediately.