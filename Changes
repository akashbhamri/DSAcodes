Praneeth, can we track this alert in the Confluence action item instead of the daily summary? The vendor is requesting a thread and heap dump, along with an increase in pool size, which might take some time. I'll keep you in the loop with each update."
