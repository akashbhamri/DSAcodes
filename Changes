You can phrase it more clearly like this:

"We have configured a synthetic alert for this controller. Could we temporarily disable it for 10 minutes to verify whether the alert triggers? If the alert is received, we will enable it for others as well."

