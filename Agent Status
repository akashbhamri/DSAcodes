#!/bin/bash

# Variables
USER="your_username"
HOST="production_server"

# Test SSH connection
ssh -i ~/.ssh/id_rsa $USER@$HOST "echo 'SSH key-based authentication successful!'"

# Check if the command was successful
if [ $? -eq 0 ]; then
    echo "Connection successful"
else
    echo "Connection failed"
fi
